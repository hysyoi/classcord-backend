apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: classcord
spec:
  selector:
    app: gateway
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080   # 固定這個號碼，nginx 才能穩定連過來
