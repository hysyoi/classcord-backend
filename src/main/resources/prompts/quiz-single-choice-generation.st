請根據以下提供的「教材片段內容」，出一題單選題（四選一）。
該題目必須針對教材片段中的核心知識點進行考核。

【教材片段內容】
{context}

【出題要求】
1. 必須是單選題，有四個選項：A、B、C、D。
2. 題目與選項必須使用繁體中文（台灣）。
3. 請確保 options 陣列中的四個字串「不含」A. B. C. D. 等選項字母前綴，請直接提供純選項內容（例如：直接提供 "選項內容" 而非 "A. 選項內容"）。
4. 請提供標準答案（即符合 JSON 格式的 ["A"]、["B"]、["C"] 或 ["D"]）。
5. 輸出格式中的 `explanation` 必須是一個 JSON 物件，包含以下兩個屬性：
   - `general` (字串)：題目的綜合解析。
   - `options` (JSON 物件/Map)：格式必須是鍵值對（Key-Value），其中鍵（Key）為大寫的 "A", "B", "C", "D"，值（Value）為該選項的詳細解析與說明。請「絕對不要」將 options 寫成陣列（Array/`[]`）格式。
6. 難易度設定為：{difficulty}。

{format_spec}

